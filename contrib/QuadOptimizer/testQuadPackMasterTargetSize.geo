// No recombination, packing or size options are set here: the command-line
// target must drive the complete PACK pipeline on a native CAD face.
SetFactory("OpenCASCADE");
Rectangle(1) = {0, 0, 0, 0.1, 0.1};
