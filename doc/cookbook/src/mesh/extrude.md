# Extruded meshes (Tutorial 3)

Use the `extrude` command to transform a mesh. We also use the `options` interface
to modify some mesh color settings.

### Python
```python
{{#include ../../../../tutorials/python/t3.py}}
```
