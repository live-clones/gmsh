// Plugin(Skin): FromMesh on the tetrahedral mesh of a cube (no view needed)

Merge "data/cube.msh";
Delete View[0]; Delete View[0]; Delete View[0]; Delete View[0];
Plugin(Skin).FromMesh = 1;
Plugin(Skin).Run;
